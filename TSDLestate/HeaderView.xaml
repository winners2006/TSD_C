<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TSDLestate.HeaderView">
    <ContentView.Content>
        <Grid BackgroundColor="LightGray" Padding="10" HorizontalOptions="FillAndExpand">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Image -->
            <Image Source="logoLestate.png"
                   HeightRequest="50"
                   VerticalOptions="Center"/>

            <!-- Title -->
            <Label Text="Lestate TSD"
                   FontSize="24"
                   VerticalOptions="Center"
                   HorizontalOptions="Start"
                   Grid.Column="1"
                   Margin="10,0,0,0"/>
        </Grid>
    </ContentView.Content>
</ContentView>
